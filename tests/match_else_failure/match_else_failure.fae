fn main() {
	func(Enum.A)
	func(Enum.B)
	func(Enum.C)
	func(Enum.D)
}

fn func(arg: Enum) {
	match arg {
		A => println("A")
		else => println("else 1")
		C => println("C")
		B => println("B")
		D => println("D")
		else => println("else 2")
	}
}

enum Enum {
	A
	B
	C
	D
}
