module main

using another_module

const THING = "Hello there!"

struct MyStruct {
	field: f64
	other_field: bool
}

fn function(): void {
	let abc = 5 * 2 + -42 / 8
	let def = 96 + { 22 }
	let character: char = 'A'
	let reference: &i32 = something
	let pointer: *i32 = something
	let slice: &[u8] = something

	//comment

	const SOMETHING: i64 = some::path::HERE

	let instance = MyStruct {
		field: 89,
		other_field: true,
	}

	return 5.0 * something_else()
}
