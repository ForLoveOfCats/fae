import fae::stdout::print_i32, print_newline

fn main() {
	print_i32(must_return(true))
	print_newline()

	print_i32(must_return(false))
	print_newline()
}

fn must_return(arg: bool): i32 {
	if arg => return 42
	else => does_not_return()
}

fn does_not_return(): noreturn {
	fae::libc::exit(0)
}
