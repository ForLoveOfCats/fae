import fae::stdout::print_string

fn main() {
	func(Enum.A)
	func(Enum.B)
	func(Enum.C)
	func(Enum.D)
}

fn func(arg: Enum) {
	match arg {
		A => print_string("A\n")
		C => print_string("C\n")
		else => print_string("else\n")
	}
}

enum Enum {
	A
	B
	C
	D
}
